apiVersion: apps/v1
kind: Deployment
metadata:
  name: insecure-bank-andres
  namespace: ag-dev
  labels:
    app: insecure-bank-andres
    env: ag-dev
spec:
  selector:
    matchLabels:
      app: insecure-bank-andres
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 3
      maxUnavailable: 1
  replicas: 3
  template:
    metadata:
      labels:
        app: insecure-bank-andres
    spec:
      containers:
      - name: insecure-bank-andres
        image: docker.io/aquasaemea/insecure-bank-andres:63
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 3000
      imagePullSecrets:
      - name: github-aquasaemea-dockerhub
#        livenessProbe:
#          httpGet:
#            path: /index.html
#            port: 3000
#            scheme: HTTP
#          initialDelaySeconds: 2
#          periodSeconds: 5
#          timeoutSeconds: 2
#        readinessProbe:
#          exec:
#            command:
#            - /bin/ls 
#            - /tmp/ready
#          initialDelaySeconds: 2
#          timeoutSeconds: 1
#        resources:
#          limits:
#            memory: "64Mi"
#            cpu: "500m"
#          requests:
#            memory: "32Mi"
#            cpu: "250m"
